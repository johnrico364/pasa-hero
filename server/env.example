# Server Configuration
PORT=3000

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Firebase Admin SDK
# Option 1: Path to service account key file
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Option 2: Service account JSON as environment variable (for production)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"pasaherodb",...}

# Firebase Project ID
FIREBASE_PROJECT_ID=pasaherodb

# Email Configuration (for sending OTP)
# 
# OPTION 1: Gmail (requires 2-Step Verification + App Password)
# 1. Enable 2-Step Verification: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the App Password below (NOT your regular Gmail password)
EMAIL_USER=your-email@gmail.com
EMAIL_APP_PASSWORD=your-16-char-app-password
EMAIL_FROM=PasaHero <noreply@pasahero-db.firebaseapp.com>
#
# OPTION 2: Custom SMTP (for other email providers like Outlook, Yahoo, etc.)
# Uncomment and configure these instead:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_PASSWORD=your-smtp-password