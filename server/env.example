# Server Configuration
PORT=3000

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Firebase Admin SDK
# Option 1: Path to service account key file
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Option 2: Service account JSON as environment variable (for production)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"pasaherodb",...}

# Firebase Project ID
FIREBASE_PROJECT_ID=pasaherodb
